{
  "rules": {
    "live_locations": {
      ".read": "auth != null && $schoolId === auth.token.schoolId",
      ".write": "auth != null && $schoolId === auth.token.schoolId"
    },
    ".write": false,
    ".read": false
  }
}
